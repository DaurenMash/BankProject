spring:
  application:
    name: history-app
  profiles:
    active: local
  kafka:
    bootstrap-servers: localhost:9092
    properties:
      security:
        protocol: PLAINTEXT
      sasl:
        enable: false
      ssl:
        enable: false
      retries:
        max: 3
      acks:
        all: true
